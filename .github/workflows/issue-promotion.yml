name: Issue Promotion

on:
  issues:
    types: [edited]

permissions:
  contents: read
  issues: write

jobs:
  promote:
    if: contains(github.event.issue.labels.*.name, 'release')
    runs-on: ubuntu-latest

    steps:
      - name: Extract SHA from label
        id: sha
        run: |
          LABELS='${{ toJson(github.event.issue.labels.*.name) }}'
          SHA=$(echo "$LABELS" | grep -o 'sha:[^"]*' | head -n1 | cut -d':' -f2)
          echo "sha=$SHA" >> $GITHUB_OUTPUT

      - name: Detect promotion checkbox
        id: detect
        run: |
          BODY="${{ github.event.issue.body }}"
          
          LINE=$(echo "$BODY" | grep "^- \[x\] " | head -n1)

          if [ -z "$LINE" ]; then
            echo "No promotion requested"
            exit 0
          fi

          ENV=$(echo "$LINE" | cut -d']' -f2 | xargs)

          ENV_LOWER=$(echo "$ENV" | tr '[:upper:]' '[:lower:]')

          echo "env=$ENV_LOWER" >> $GITHUB_OUTPUT


      - name: Mark Deploying
        uses: actions/github-script@v7
        with:
          script: |
            const issue = context.payload.issue
            const env = "${{ steps.detect.outputs.env }}"
            const upper = env.toUpperCase()

            let body = issue.body

            const regex = new RegExp(`- \\[x\\] .*${upper}`)
            body = body.replace(
              regex,
              `🔁 ${upper}`
            )

            await github.rest.issues.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issue.number,
              body: body
            })

      - name: Deploy via reusable workflow
        uses: ./.github/workflows/deploy-reusable.yml
        with:
          environment: ${{ steps.detect.outputs.env }}
          sha: ${{ steps.sha.outputs.sha }}

      - name: Mark Done
        if: success()
        uses: actions/github-script@v7
        with:
          script: |
            const issue = context.payload.issue
            const env = "${{ steps.detect.outputs.env }}"
            const upper = env.toUpperCase()

            let lines = issue.body.split("\n")

            const index = lines.findIndex(line =>
              line.trim().startsWith(`🔁 ${upper}`)
            )

            if (index === -1) {
              core.info("Deploying line not found")
              return
            }

            lines[index] = `✅ ${upper}`

            if (lines[index + 1]) {
              const nextLine = lines[index + 1].trim()

              if (nextLine.startsWith("🚫 ")) {
                const nextEnv = nextLine.replace("🚫 ", "").trim()
                lines[index + 1] = `- [ ] ${nextEnv}`
              }
            }

            const newBody = lines.join("\n")

            await github.rest.issues.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issue.number,
              body: newBody
            })
